// Phase 6：benchmark 範例
fn fib(n: int) -> int {
    if n < 2 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

let sum = 0;
let i = 1;

while i <= 100000 {
    sum = sum + i;
    i = i + 1;
}

print(fib(30));
print(sum);

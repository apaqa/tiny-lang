// Phase 5：match 範例
let x = 2;

match x {
    1 => { print("one"); }
    2 => { print("two"); }
    other => { print(other); }
}

match "tiny" {
    "lang" => { print("lang"); }
    _ => { print("fallback"); }
}
